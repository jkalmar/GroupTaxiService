const db = require('./database');

const sqlGetAllUsers = "SELECT id, username, phone FROM customers"











/**
 *
 * @param {Express.Router} app
 */
function init( app ) {

}


module.exports = {
    init
}
